/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['lh3.googleusercontent.com'], // Add your image host domain(s) here
      },
}

module.exports = nextConfig
